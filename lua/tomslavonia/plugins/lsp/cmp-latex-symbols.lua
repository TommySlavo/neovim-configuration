return {
  "kdheepak/cmp-latex-symbols",
  config = function()
    -- No explicit config required here unless you want to set global options
    -- You can configure the strategy in your nvim-cmp setup instead
  end,
}
